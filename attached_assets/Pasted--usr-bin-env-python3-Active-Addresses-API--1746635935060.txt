#!/usr/bin/env python3
"""
Скрипт для ежедневного мониторинга Active Addresses через бесплатный API Blockchair.
Хранит историю в CSV-файлах и сравнивает текущее значение с 7-дневным средним.
"""
import requests
import csv
import os
import time
import statistics

# --- Настройки ---
CHAINS = ['bitcoin', 'ethereum']  # поддерживаемые цепочки
SYMBOL_MAP = {'bitcoin': 'BTC', 'ethereum': 'ETH'}
HISTORY_DIR = 'history'            # директория для файлов истории
DAYS_FOR_AVG = 7                   # скользящее окно в днях

# Пороги (%) для интерпретации vs 7-дневного среднего
THRESHOLDS = [(-10, 'Очень низкий спрос'),
              (-2,  'Ослабевший спрос'),
              (2,   'Нормальный уровень'),
              (10,  'Повышенный спрос'),
              (100, 'Очень высокий спрос')]

# Создаём папку для истории, если её нет
os.makedirs(HISTORY_DIR, exist_ok=True)

def fetch_active_addresses(chain: str) -> int:
    """
    Возвращает число активных адресов сети за последние 24 часа без ключей.
    """
    url = f'https://api.blockchair.com/{chain}/stats'
    resp = requests.get(url)
    resp.raise_for_status()
    data = resp.json().get('data', {})
    return data.get('active_addresses', 0)


def history_file(chain: str) -> str:
    """Путь к CSV-файлу истории для цепочки."""
    return os.path.join(HISTORY_DIR, f'history_{chain}.csv')


def read_history(chain: str) -> list[int]:
    """Читает сохранённую историю (список значений) из CSV."""
    path = history_file(chain)
    if not os.path.exists(path):
        return []
    with open(path, newline='') as f:
        reader = csv.reader(f)
        return [int(row[1]) for row in reader if len(row) >= 2]


def append_history(chain: str, value: int):
    """Добавляет сегодняшнее значение в конец CSV, если его там ещё нет."""
    path = history_file(chain)
    today = time.strftime('%Y-%m-%d')
    rows = []
    if os.path.exists(path):
        with open(path, newline='') as f:
            rows = list(csv.reader(f))
        if rows and rows[-1][0] == today:
            return  # уже добавлено
    with open(path, 'a', newline='') as f:
        writer = csv.writer(f)
        writer.writerow([today, value])


def interpret_vs_avg(today: int, avg: float) -> (float, str):
    """
    Сравнивает today's со средним avg, возвращает дельту в % и текстовую метку.
    """
    if avg == 0:
        return 0.0, 'Недостаточно данных'
    delta_pct = (today - avg) / avg * 100
    for thresh, label in THRESHOLDS:
        if delta_pct <= thresh:
            return delta_pct, label
    return delta_pct, THRESHOLDS[-1][1]


def main():
    print(f"\n📊 Active Addresses Monitor (Blockchair) — {time.strftime('%Y-%m-%d')}\n")
    for chain in CHAINS:
        symbol = SYMBOL_MAP.get(chain, chain.upper())
        # Читаем историю и вычисляем среднее за DAYS_FOR_AVG дней
        hist = read_history(chain)
        recent = hist[-DAYS_FOR_AVG:] if len(hist) >= DAYS_FOR_AVG else hist
        avg7 = statistics.mean(recent) if recent else 0

        # Получаем текущее значение
        try:
            today = fetch_active_addresses(chain)
        except Exception as e:
            print(f"Ошибка при запросе {symbol}: {e}")
            continue

        # Интерпретация против среднего
        delta_pct, label = interpret_vs_avg(today, avg7)

        # Печать результатов
        print(f"• {symbol}: {today:,} адресов")
        if avg7:
            print(f"  Среднее за {len(recent)} дн: {avg7:,.0f} → Δ {delta_pct:+.1f}% — {label}")
        else:
            print("  Недостаточно данных для вычисления среднего.")

        # Сохраняем сегодняшнее значение
        append_history(chain, today)

if __name__ == '__main__':
    main()
